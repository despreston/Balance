import { StyleSheet } from 'react-native';
import Colors from '../colors';

export default StyleSheet.create({

  text: {
    marginTop: 20,
    color: Colors.blue,
    fontSize: 16,
    fontWeight: 'bold',
    textAlign: 'center'
  }

});