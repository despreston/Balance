import { StyleSheet } from 'react-native';

export default StyleSheet.create({

  touchable: {
    justifyContent: 'center',
    height: 30,
    width: 30
  },

  image: {
    height: 20,
    width: 20
  }

});